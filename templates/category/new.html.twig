{% extends "base.html.twig" %}


{% block body %}

   if ($form->isSubmitted() && $form->isValid()) {

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button type="submit">Add category</button>
	{{ form_end(form) }}
   }

{% endblock %}
